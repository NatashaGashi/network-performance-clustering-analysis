# network-performance-clustering-analysis
 Dataset that cleans and engineers features from the AI6G QoE dataset, performs clustering analysis, and then uses a Genetic Algorithm (GA) to select feature subsets that improve clustering performance.
